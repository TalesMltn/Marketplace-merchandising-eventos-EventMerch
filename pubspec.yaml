name: flutter_application_1
description: "A new Flutter project."

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter

  # SUPABASE + AUTH + REALTIME
  supabase_flutter: ^2.8.0

  # NAVEGACIÓN
  go_router: ^14.2.0

  # ESTADO
  flutter_riverpod: ^2.5.1

  # IMÁGENES CON CACHE
  cached_network_image: ^3.3.1

  # GENERAR QR
  qr_flutter: ^4.1.0

  # COMPARTIR
  share_plus: ^12.0.1

  # UUID (solo una vez, aquí)
  uuid: ^4.5.0

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/